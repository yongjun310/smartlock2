<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
 
 	<style name="ShadowStyle">
        <item name="android:shadowColor">#4d4d4d</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">-3</item>
        <item name="android:shadowRadius">3</item>
    </style>
 
 	<style name="TextShadowStyle">
        <item name="android:shadowColor">#66000000</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">4</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="ThemeLockActivity">
        <item name="android:windowAnimationStyle">@style/AnimationLockActivity</item>
        <item name="android:windowNoTitle">true</item>
    </style>


    <style name="ThemeActivity">
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AnimationLockActivity" parent="@style/Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowExitAnimation">@anim/anim_exit_lock_activity</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_exit_lock</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_exit_lock</item>

        <item name="android:taskOpenEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:taskOpenExitAnimation">@anim/anim_exit_lock</item>
        <item name="android:taskCloseEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:taskCloseExitAnimation">@anim/anim_exit_lock</item>

        <item name="android:taskToFrontEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:taskToFrontExitAnimation">@anim/anim_exit_lock</item>
        <item name="android:taskToBackEnterAnimation">@anim/anim_enter_lock</item>
        <item name="android:taskToBackExitAnimation">@anim/anim_exit_lock</item>
        
    </style>

    <style name="myTransparent" parent="@style/Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>

    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="CustomProgressDialog" parent="@style/CustomDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="ShareDialog" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/anim_enter_dialog</item>
        //进入时的动画
        <item name="@android:windowExitAnimation">@anim/anim_exit_dialog</item>
        //退出时的动画
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="DialogText">
        <item name="android:textColor">#FF000000</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="DialogText.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonGreen" parent="@android:style/Widget.Button">
        <item name="android:textSize">18.0sp</item>
        <item name="android:textColor">@drawable/skin_color_button</item>
        <item name="android:background">@drawable/common_btn_green</item>
    </style>

    <style name="ButtonRed" parent="@android:style/Widget.Button">
        <item name="android:textSize">18.0sp</item>
        <item name="android:textColor">@drawable/skin_color_button</item>
        <item name="android:background">@drawable/common_btn_red</item>
    </style>

    <style name="OverlayDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
